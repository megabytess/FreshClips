import 'package:flutter/material.dart';

class AdminVerifiedDeclinedPage extends StatelessWidget {
    @override
    Widget build(BuildContext context) {
        return Scaffold(
            appBar: AppBar(
                title: Text('Admin Verified Declined Page'),
            ),
            body: Center(
                child: Text('Content goes here'),
            ),
        );
    }
}