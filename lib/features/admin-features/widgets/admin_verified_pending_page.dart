import 'package:flutter/material.dart';

class AdminVerifiedPendingPage extends StatelessWidget {
  const AdminVerifiedPendingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
